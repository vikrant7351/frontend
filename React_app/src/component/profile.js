import React from "react";

const Profile = () => {
    return(
        <h1 className='message' style={{color:'red',textAlign:'center'}}>  welcome to my profile</h1>
    );
};

export default Profile;